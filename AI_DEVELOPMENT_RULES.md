# AI 开发规则

> 本文件用于所有 AI Coding / Vibe Coding 修改。
>
> AI 在修改代码前必须阅读：
>
> 1. `ARCHITECTURE.md`
> 2. 本文件
> 3. 与当前需求直接相关的代码

---

# 1. 第一原则：先理解，后修改

禁止看到需求后立即写代码。

必须先：

```text
阅读相关代码
↓
理解现有职责
↓
确认调用关系
↓
确认数据流
↓
判断功能归属
↓
再修改
```

---

# 2. 不允许默认“就改 app.js”

`app.js` 是应用入口与装配层。

它不是万能模块。

如果需求属于：

```text
问诊
游戏
存储
备份
病例
答案判断
账号
```

必须优先进入对应模块。

---

# 3. 每个功能必须有唯一主要归属模块

新增功能前必须回答：

```text
这个功能的主要职责是什么？
哪个模块拥有这个职责？
```

不要因为某个文件“刚好方便修改”就把代码放进去。

---

# 4. 不允许职责漂移

一个模块已经承担 A 职责时：

不能因为实现方便，再加入：

```text
B
C
D
```

例如：

```text
Storage
不能顺便负责 UI

Game
不能顺便负责账号

UI
不能顺便负责答案判定

Data
不能顺便负责游戏状态
```

---

# 5. 不允许跨模块直接碰内部实现

禁止：

```js
otherModule.someInternalVariable
```

禁止直接修改其他模块内部状态。

必须使用公开接口。

---

# 6. 不允许绕过模块边界

禁止：

```text
UI → localStorage
UI → D1
Game → D1
Game → localStorage
Domain → DOM
Domain → window
Domain → fetch
```

除非 `ARCHITECTURE.md` 明确规定该模块拥有此职责。

---

# 7. 不允许复制已有逻辑

新增代码前搜索：

```text
是否已有相同功能？
是否已有相同工具？
是否已有相同验证？
是否已有相同状态？
是否已有相同数据转换？
```

存在则复用。

禁止为了“当前文件写起来方便”复制一份。

---

# 8. 不允许猜测

当不知道：

```text
API 字段
返回格式
参数名称
数据结构
接口职责
第三方行为
```

不得猜。

禁止：

```js
a?.b?.c || a?.d || a?.e || fallback
```

这种未经契约确认的兼容写法。

必须：

```text
查看现有实现
查看真实调用方
查看明确文档
```

仍不明确时，应指出不确定点。

---

# 9. 不允许为了保证运行而写大量兜底

错误：

```text
任何异常都返回空数组
任何字段不存在就使用默认值
多个可能字段轮流尝试
多个接口格式轮流兼容
捕获异常后继续正常运行
```

正确：

```text
输入符合契约 → 正常运行

输入违反契约 → 明确失败
```

---

# 10. 错误必须可定位

错误信息应该帮助定位问题。

例如：

```text
病例 basic-003 缺少 inquiry.questions
```

优于：

```text
加载失败
```

错误应该说明：

```text
哪个模块
哪个对象
哪个字段
哪个阶段
```

---

# 11. 不允许静默吞错

不要为了让页面“不报错”而：

```js
catch {
    return [];
}
```

除非该失败属于已经定义好的业务状态。

否则应该明确抛错或向上层返回失败。

---

# 12. 函数必须可预测

函数名应该准确表达职责。

推荐：

```text
getCompletedCases()
markCaseCompleted()
evaluateAnswer()
matchInquiry()
loadCaseData()
saveProgress()
restoreBackup()
```

避免：

```text
handle()
process()
run()
doSomething()
update()
manage()
```

除非上下文非常明确。

---

# 13. 减少隐式副作用

一个函数如果会：

```text
修改 DOM
修改 Storage
发请求
改变全局状态
弹窗
```

必须明确。

尽量避免：

```text
看起来像计算函数
实际上偷偷修改五个地方
```

---

# 14. 纯逻辑保持纯逻辑

如果一个函数只是：

```text
输入
→ 计算
→ 返回结果
```

就不要加入：

```text
DOM
Storage
Network
Alert
Navigation
```

---

# 15. 状态必须有唯一所有者

修改状态前先确定：

> 谁拥有这个状态？

例如：

```text
当前病例
→ Game Session

登录用户
→ Auth Session

完成记录
→ Progress Store

题库筛选
→ Case Bank

问诊匹配规则
→ Inquiry Matcher
```

其他模块只能通过接口访问。

---

# 16. 不允许多个模块共同维护同一事实

禁止：

```text
A 保存一份完成记录
B 再保存一份完成记录
C 再维护一个完成数量
```

应该：

```text
Progress
 ↓
统一读取
```

---

# 17. 修改公共接口前必须检查所有调用方

如果修改：

```js
someFunction(...)
```

必须搜索所有调用位置。

不得只改定义，不检查使用方。

不得为了兼容旧调用永久增加一堆兼容参数。

---

# 18. 不允许无意义的兼容层

禁止长期保留：

```text
旧接口
+
新接口
+
兼容旧接口的 wrapper
+
fallback
```

除非有明确的迁移需求和结束时间。

---

# 19. 新功能优先复用已有架构

先寻找：

```text
现有模块
现有服务
现有状态
现有接口
现有工具
```

确认不能复用后，才创建新代码。

---

# 20. 不允许为了未来提前实现

例如账号系统还未开发：

禁止提前写：

```text
fakeAuth
mockUser
fakeToken
placeholderD1
futureSync
```

不要因为“以后可能用”而增加当前复杂度。

只需要保证当前边界合理。

---

# 21. 新增账号系统时的特殊规则

未来账号系统接入时：

Auth 只负责：

```text
注册
登录
登出
会话
当前用户身份
```

Progress 只负责：

```text
完成病例
错题
学习数据
```

Game 只负责：

```text
游戏流程
```

Case Bank 只负责：

```text
题库展示与操作
```

不得让登录逻辑散落到所有模块。

---

# 22. 不允许 Game 直接依赖具体账号实现

错误：

```text
game.js
→ localStorage user
→ D1
→ token
→ session
```

正确：

```text
game
 ↓
progress service / auth service
 ↓
具体实现
```

Game 只关心：

```text
当前用户是否存在
是否允许保存
```

不关心：

```text
Token 怎么存
D1 怎么查
Session 怎么刷新
```

---

# 23. 修改范围必须最小

需求：

> 修复问诊匹配。

优先修改：

```text
inquiry-matcher
inquiry
病例数据
相关测试
```

不要顺便：

```text
重构 Storage
改变导航
重做 UI
改变账号逻辑
修改病例格式
```

---

# 24. 发现架构问题时，不要偷偷绕过

如果当前架构阻碍需求：

必须明确指出：

```text
问题：
A 模块目前同时承担 X / Y。

本次需求需要 X。

继续把代码塞进 A 会扩大职责。

建议：
将 Y 移出 A。
```

然后再实施。

禁止使用：

```text
临时变量
兼容函数
全局变量
fallback
```

来逃避结构问题。

---

# 25. 不允许“大重构顺便做”

用户要求修一个 Bug 时，不得顺便：

```text
重写项目架构
升级技术栈
替换依赖
重写所有 UI
修改所有数据结构
```

除非这些改变是正确修复该问题的必要条件。

---

# 26. 每次修改后必须进行四项检查

## A. 功能检查

需求是否真正完成？

## B. 依赖检查

是否增加了新的跨模块依赖？

## C. 职责检查

新增代码是否放在正确模块？

## D. 冗余检查

是否新增了：

```text
重复函数
重复状态
重复工具
重复数据
无意义兼容
无意义 fallback
```

---

# 27. 每次修改后必须回答以下问题

```text
本次修改了哪些文件？

每个文件为什么需要修改？

新增的职责是什么？

有没有修改已有接口？

有没有新增模块依赖？

有没有新增全局变量？

有没有新增 fallback？

有没有复制已有逻辑？

有没有遗留废弃代码？

未来出现问题时，应首先检查哪个模块？
```

---

# 28. 如果没有必要，不要创建新模块

拆模块的理由必须是：

```text
职责不同
+
维护需要
+
边界明确
```

不能仅仅因为：

```text
文件看起来比较长
```

就拆成大量没有实际意义的小文件。

---

# 29. 如果模块明显过大，优先按职责拆

例如：

错误：

```text
storage1.js
storage2.js
storage3.js
```

正确：

```text
progress-storage.js
backup-service.js
backup-code.js
```

拆分依据是职责，不是字符数。

---

# 30. 删除代码比继续兼容更重要

如果旧功能已经废弃：

必须优先删除：

```text
废弃函数
废弃变量
废弃接口
废弃 UI
废弃兼容逻辑
```

不要长期保存：

```text
“万一以后还有用”
```

的代码。

---

# 31. 不要制造隐藏依赖

尤其禁止依赖：

```text
window 全局变量
DOM 中隐藏状态
localStorage 中未声明 key
模块加载顺序副作用
未文档化的全局函数
```

模块依赖应该能够通过 import / export 或明确接口看出来。

---

# 32. 不要把修复问题变成增加复杂度

修复一个错误时，优先：

```text
找到原因
↓
修复原因
↓
删除不必要的 workaround
```

而不是：

```text
出现问题
↓
加一个 if
↓
再加一个 fallback
↓
再加一个特殊判断
```

---

# 33. 不允许针对单个病例 ID 写业务特判

例如：

```js
if (caseId === 'basic-003') ...
```

除非这个 ID 本身就是业务规则的一部分，并且已经在架构文档中明确规定。

病例差异应尽量通过：

```text
病例数据
配置
规则
```

表达，而不是：

```text
代码里的 case.id 特判
```

---

# 34. 数据优先于代码特判

如果某个病例需要：

```text
新的问诊维度
新的舌象字段
新的显示方式
```

先判断：

> 是否应该由病例数据表达？

如果属于病例自身差异，应优先修改数据结构和明确的数据契约，而不是把特殊情况写死在业务代码里。

---

# 35. 保持可读性

不要为了少写几行代码使用过度压缩写法。

优先：

```text
清晰
直接
容易阅读
容易定位
```

而不是：

```text
一行塞十个逻辑
```

---

# 36. 不过度设计

不要因为“架构原则”而引入：

```text
复杂设计模式
事件总线
状态管理框架
抽象工厂
过度 Repository
多层 Adapter
复杂依赖注入
```

除非项目实际问题已经证明需要。

原则：

> **简单需求用简单结构解决。**

---

# 37. 最终检查标准

每次代码完成后，必须确认：

```text
[ ] 功能正确

[ ] 没有猜测接口

[ ] 没有无依据 fallback

[ ] 没有冗余代码

[ ] 没有复制逻辑

[ ] 没有新增不必要依赖

[ ] 没有跨模块修改内部状态

[ ] 没有破坏已有接口

[ ] 没有职责漂移

[ ] 没有产生隐藏全局状态

[ ] 没有保留废弃代码

[ ] 错误能够明确暴露

[ ] 未来发生问题时能够定位模块
```

---

# 38. AI 的最终工作原则

永远遵循：

```text
先理解
→ 再定位
→ 再设计
→ 最小修改
→ 验证
→ 检查架构
→ 清理冗余
```

而不是：

```text
看到需求
→ 马上写代码
→ 报错
→ 加 fallback
→ 再报错
→ 再加兼容
→ 最终能跑就算完成
```

本项目追求的是：

> **可维护，而不仅仅是可运行。**

以及：

> **让未来的人和 AI 都能知道代码为什么这样组织。**

---

# 39. 架构检查（必做）

每次修改代码完成后必须运行：

```bash
node tools/arch-check.mjs
```

必须确认：

```text
无 import 断链
无循环依赖
无未声明模块
无反向依赖
无越界 API
无失效规则
```

如果新增 `js/**` 业务模块，必须同时：

```text
1. 在 ARCHITECTURE.md 记录其职责
2. 在 tools/arch-check.mjs 的 LAYER 中声明层级
3. 通过 arch-check
```

禁止：

```text
功能测试通过
但架构检查失败
```

仍然提交。

---

# 40. 测试运行（必做）

每次修改代码完成后必须运行：

```bash
npm test
```

不得只运行新增加的测试，必须运行现有测试套件。

如果本次修改涉及某个专项模块，则必须确认相关专项测试也通过。

最终报告必须说明：

```text
npm test：通过 / 失败
arch-check：通过 / 失败
```

失败必须说明具体原因。

---

# 41. 完成条件

```text
代码修改完成 ≠ 任务完成。
```

只有：

```text
功能实现
+
架构检查通过
+
现有测试通过
+
修改范围核对完成
```

才视为本次修改完成。
